import '@testing-library/jest-dom'
/*
import { vi } from "vitest";

// ✅ MOCK BEFORE ANY IMPORTS
vi.mock("@/server/services/auth.service", () => ({
  authService: {
    login: vi.fn(),
    logout: vi.fn(),
    me: vi.fn(),
    register: vi.fn(),
    refresh: vi.fn(),
    requestOtp: vi.fn(),
    verifyOtp: vi.fn(),
    forgotPassword: vi.fn(),
    resetPassword: vi.fn(),
  },
}));

// Optional (if using wrappers)
vi.mock("@/server/http/route-helpers", () => ({
  withErrorHandling: (fn: any) => fn,
  withAuth: (fn: any) => fn,
}));
*/